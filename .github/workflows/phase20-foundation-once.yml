name: Phase 20 Foundation Once

on:
  push:
    branches:
      - phase-20-publication-epub-roundtrip
    paths:
      - .github/workflows/phase20-foundation-once.yml

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: phase-20-publication-epub-roundtrip
          fetch-depth: 0
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt,clippy
      - name: Apply migration
        run: python3 .github/scripts/phase20_foundation.py
      - name: Format
        working-directory: engine
        run: |
          cargo fmt --all
          cargo fmt --all -- --check
      - name: Check workspace
        working-directory: engine
        run: cargo check --locked --workspace
      - name: Clippy affected crates
        id: clippy
        continue-on-error: true
        working-directory: engine
        run: cargo clippy --locked -p translation-core -p document-engine -p literary-review-engine -p project-engine -p literary-engine --all-targets -- -D warnings 2>&1 | tee ../phase20-foundation-clippy.log
      - name: Persist clippy diagnostic
        if: steps.clippy.outcome == 'failure'
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add phase20-foundation-clippy.log
          git commit -m 'chore: capture Phase 20 foundation clippy diagnostic'
          git push origin HEAD:phase-20-publication-epub-roundtrip
          exit 1
      - name: Test affected crates
        working-directory: engine
        run: cargo test --locked -p translation-core -p document-engine -p literary-review-engine -p project-engine -p literary-engine
      - name: Remove temporary migration tooling
        run: rm -f .github/scripts/phase20_foundation.py .github/workflows/phase20-foundation-once.yml phase20-foundation-check.log phase20-foundation-clippy.log
      - name: Commit validated foundation
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'feat: add adult intimacy fidelity foundation and EPUB block provenance'
          git push origin HEAD:phase-20-publication-epub-roundtrip

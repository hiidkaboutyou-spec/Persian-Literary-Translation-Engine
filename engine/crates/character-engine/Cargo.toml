[package]
name = "character-engine"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
text-normalization = { path = "../text-normalization" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[package]
name = "document-engine"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
pdf-extract = "0.12"
zip = { version = "2", default-features = false, features = ["deflate"] }

[package]
name = "document-engine"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
quick-xml = "0.37"
zip = { version = "2.2", default-features = false, features = ["deflate"] }
